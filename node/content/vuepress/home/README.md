# home

## 一、介绍

```js
// 每一个 Markdown 文件，首先都会编译为 HTML

// 然后转换为一个 Vue 单文件组件 (SFC) 。
// 换句话说，你可以把 Markdown 作为 Vue SFC 来看待：

// 思路
// 1、根据配置
// 2、使用markdown-it把md生成html
// 3、再转化为vue

// 所以
// 教程基本就是围绕：
// 配置（markdown解析配置，站点配置、打包配置）

// 至于md文件，可以看下提供的一些默认组件，md其它语法
// 其它<scipt>和style外的，会处理成template。所以当成vue来写就好了。
```

## md例子